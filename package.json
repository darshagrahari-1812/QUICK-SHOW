{
  "name": "quick-show",
  "version": "1.0.0",
  "scripts": {
    "server": "npm run server --prefix server",
    "client": "npm run dev --prefix client"
  },
  "dependencies": {
    "lucide-react": "^1.30.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
